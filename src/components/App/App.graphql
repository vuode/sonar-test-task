query Launches($limit: Int!, $offset: Int!) {
  launchesPastResult(limit: $limit, offset: $offset) {
    data {
      id
      mission_name
      launch_date_local
      rocket {
        rocket_name
      }
      details
    }
    result {
      totalCount
    }
  }
}
